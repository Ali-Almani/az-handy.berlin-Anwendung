# Lokal mit Vite-Proxy: leer lassen oder weglassen (Anfragen gehen an /api).
# Direkt gegen Node ohne Proxy: VITE_API_URL=http://localhost:5000/api
# Production-Build auf Server: nicht setzen → /api unter derselben Domain wie das Frontend (Nginx).
# VITE_API_URL=
